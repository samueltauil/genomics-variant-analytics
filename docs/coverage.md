# Capability coverage

This is the source of truth for what the repository can currently support in a presentation. It compares the ten capability specs with implementation evidence that exists in this repository.

OpenSpec implementation progress: **65/89 tasks complete**.

## Status definitions

| Status | Meaning |
|---|---|
| Demonstrated | Runnable behavior exists and can be shown producing the observable results required by the capability spec. |
| Partially demonstrated | Some capability requirements have inspectable or runnable evidence, but the capability is not complete. Show only the evidence named here. |
| Specified only | The repository contains the behavior contract, but no implementation evidence. Describe it only as architectural intent. |

Documentation, diagrams, and specifications are not evidence that data-plane behavior runs. A presenter may show a repository artifact only where the table explicitly identifies that artifact as partial evidence.

## Coverage table

| Capability | Status | Current evidence | Presenter guidance |
|---|---|---|---|
| [`ingestion/smb-landing-zone`](../openspec/changes/add-genomics-variant-accelerator/specs/ingestion/smb-landing-zone/spec.md) | **Partially demonstrated** | The tracked [deployment record](../.azure/deployment-plan.md#verified-results), parameterized [landing-zone Bicep](../infra/modules/landing-zone.bicep), and [acceptance script](../scripts/Test-Environment.ps1) record a disposable SSD provisioned-v2 share with SMB Multichannel, managed-identity mounting, a 100 GiB sequential write measured at 240 MiB/s, and the 3000 IOPS/200 MiB/s provisioned ceiling. The [landing inventory](landing-inventory.md) has runnable local evidence for scheduled discovery, the two-observation or marker completeness rule, and [failure detection and retry](landing-inventory.md#failure-detection-and-retry) against a declared transfer manifest. Access tests record the processing identity denied a landing-share listing. No real instrument was joined, unchanged instrument configuration and realistic folder seeding are unverified, and no interrupted transfer has been observed over SMB or from an instrument. The cloud environment's current existence was not re-verified during this reconciliation. | Show the tracked acceptance measurements and run the local scanner only within their stated scope, including the synthetic truncated-transfer and retry sequence. Describe the share as a previously tested disposable foundation, not a currently available environment or proof of instrument compatibility. Do not claim unchanged lab configuration, demo-data seeding, a complete ingestion flow, or that failure detection works without the sending run declaring an expected size. |
| [`ingestion/object-storage-staging`](../openspec/changes/add-genomics-variant-accelerator/specs/ingestion/object-storage-staging/spec.md) | **Partially demonstrated** | [Data Factory Bicep](../infra/modules/staging.bicep) defines a managed-virtual-network Copy pipeline using separate ingestion and staging identities. The checked-task record and [staging acceptance script](../scripts/Test-Staging.ps1) record a disposable run that copied the stable file and skipped a growing file. The [staging log](../scripts/stage_records.py), its focused tests, and the [integrity acceptance script](../scripts/Test-StagingIntegrity.ps1) cover SHA-256 comparison, the six required status fields, and withholding a deliberately corrupted destination. Purview registration/lineage and Storage Actions lifecycle tiering are absent; classification is a declared label, not implemented classification policy. The cloud environment's current existence was not re-verified. | Show the Copy definition, local staging-log behavior, and tracked disposable-run results. Do not present the environment as currently deployed or claim Purview lineage, lifecycle transitions, classification policy, or end-to-end downstream availability. |
| [`processing/secondary-analysis`](../openspec/changes/add-genomics-variant-accelerator/specs/processing/secondary-analysis/spec.md) | **Specified only** | The processing stages and execution targets are defined in the spec. There is no workflow definition, container, Batch or HPC configuration, run record, output artifact, or concordance result. | Present Batch and HPC as alternative proposed targets. Do not claim FASTQ-to-VCF execution, equivalent outputs, provenance capture, failure handling, or elastic scale-down. |
| [`variant-store/delta-variant-store`](../openspec/changes/add-genomics-variant-accelerator/specs/variant-store/delta-variant-store/spec.md) | **Partially demonstrated** | The local [SQLite Bronze harness](variant-store.md), `scripts/variant_store.py`, and focused synthetic tests implement the exact 20-field contract, VCF parsing, mandatory-field rejection, null annotations, run/reference provenance, rejected records, idempotency, reprocessing separation, shortcut metadata resolution, and inspectable layout/maintenance metadata. It is not a deployed Delta table, OneLake shortcut, ADLS tier transition, or production optimization run. | Show only the local synthetic parser/store behavior and keep the exact VCF core and context-field selection labelled as an accelerator assumption. Do not claim deployed Delta, cloud tiering, production layout validation, or representative-volume performance. |
| [`variant-store/genomic-metadata`](../openspec/changes/add-genomics-variant-accelerator/specs/variant-store/genomic-metadata/spec.md) | **Partially demonstrated** | The local [SQLite metadata model](metadata-store.md) and focused tests implement synthetic Subject-to-variant traversal in both directions, required file metadata, transactional referential integrity, and metadata-only archival while preserving ancestry. It is a trusted local API, not a deployed or access-controlled service; clinical/research grants, subject-linkage filtering, pipeline integration, payload archival, audit, and Delta/Purview integration remain absent. | Demonstrate only the synthetic local model and label its URI, producer, and integrity values as recorded metadata. Do not claim governed service access, pipeline-produced lineage, storage archival, clinical/research separation, or patient-linked authorization. |
| [`reference-data/reference-management`](../openspec/changes/add-genomics-variant-accelerator/specs/reference-data/reference-management/spec.md) | **Partially demonstrated** | The [reference publisher](../scripts/publish_reference.py) and focused tests implement `type/name/version` publication, per-artifact SHA-256 manifests, inventory listing, and refusal to republish an existing version. The checked-task [verification record](../openspec/changes/add-genomics-variant-accelerator/tasks.md#4-reference-data) and [acceptance script](../scripts/Test-ReferenceData.ps1) record five versioned Ensembl artifacts published in a disposable reference container, a WORM-policy overwrite rejected with `409 BlobImmutableDueToPolicy`, and a successor version leaving its predecessor retrievable. [Submission-time compatibility validation](../docs/reference-submission.md) and [audited publish/read authorization](governance-local-controls.md#reference-publish-and-read) have local tests, including a denied publish that leaves the zone unchanged. The Azure client drivers still publish with an explicit `None` governor, so cloud-side reference auditing is not implemented. The cloud environment's current existence was not re-verified. | Show the local publisher, compatibility, and audit tests and the tracked disposable publication/immutability results. Name the published entries exactly as recorded; do not relabel GRCh37 as a tested hg19 distribution. Do not claim current cloud availability or that a deployed publish is audited. |
| [`governance/access-and-lineage`](../openspec/changes/add-genomics-variant-accelerator/specs/governance/access-and-lineage/spec.md) | **Partially demonstrated** | The tracked infrastructure defines separate managed identities, scoped storage roles, private endpoints, private DNS, disabled shared-key access, and private Data Factory connectivity. The [deployment record](../.azure/deployment-plan.md#verified-results) records staging write access, processing read access, processing write denial, and landing-share denial in a disposable environment. Task 8.2 is now checked: every service-to-service path in the IaC and scripts is managed-identity-only, no component requires an external secret, and a new [repository secret scanner](../scripts/scan_secrets.py) with [focused tests](../scripts/../tests/test_scan_secrets.py) confirms no embedded connection string or storage key, wired into the existing required data-hygiene check. See [governance-local-controls.md#identity-based-service-access-and-secret-handling](governance-local-controls.md#identity-based-service-access-and-secret-handling). The staging log carries a fixed declared classification label. The four-tier model, de-identified projections, clinical/research separation, classification policy, immutable audit, and the controlled external-sharing approval gate are implemented locally (tasks 8.1, 8.3-8.6, 8.8): [share_externally](governance-local-controls.md#external-sharing-approval-gate) blocks a share without a matching, unexpired, unused approval and audits both the denial and the approved execution as tamper-evident entries in the same hash-chained trail. Purview lineage remains absent, and no fresh deployment re-verified these paths because `rg-genomics-demo` is currently absent. | Present these as previously exercised storage-boundary building blocks plus checked local governance/identity controls, not a currently deployed or fully governed environment. Do not claim a current deployment, compliance, full private end-to-end operation, or end-to-end lineage; present the sharing gate as a locally demonstrated code path, not a deployed enforcement boundary. |
| [`analytics/variant-query-and-visualization`](../openspec/changes/add-genomics-variant-accelerator/specs/analytics/variant-query-and-visualization/spec.md) | **Partially demonstrated** | The local [governed analytics harness](analytics-query.md), `scripts/analytics_query.py`, `scripts/analytics_experience.py`, and focused synthetic tests implement all six query scenarios, access-aware projections, equivalent notebook-style and SQL-adapter results, per-row traceability, snapshot reruns, six view models, measured local response time, and bounded AI-assisted exploration through the governed query engine. The 1,000 ms target and screen set remain assumptions; no Fabric/Databricks notebook, SQL warehouse, dashboard, or governed AI service is deployed. | Show the local synthetic query/view/assistant behavior and its exploratory label. Keep the target, screen set, scenario, and service choices labelled as assumptions. Do not present local adapters or view models as deployed Azure analytics services or clinical interpretation. |
| [`platform/engineering-workflow`](../openspec/changes/add-genomics-variant-accelerator/specs/platform/engineering-workflow/spec.md) | **Partially demonstrated** | The required [data-hygiene workflow](../.github/workflows/data-hygiene.yml), scanner, and synthetic tests are tracked; [live acceptance records](../CONTRIBUTING.md#live-acceptance-record-2026-09-10) document forbidden-file merge refusal under required checks and the limits of the current zero-approval policy. A separate [secret-protection record](../CONTRIBUTING.md#secret-protection-acceptance-record-2026-09-10) documents a recognized synthetic credential rejected at push time. Repository-resident AI context includes [Copilot instructions](../.github/copilot-instructions.md), [prompt files](../.github/prompts/), and the [genomics variant-store skill](../.github/skills/genomics-variant-store/SKILL.md). The [engineering controls](engineering-controls.md) add a trusted automated PR review, deterministic scheduled-failure triage logic with dry-run and GitHub adapters, a tier/visibility control matrix, and a 2026-09-19 live synthetic acceptance record: issue #26 was created with required provenance, updated in place for a second occurrence, verified as the sole issue for its marker, and closed after capture. Local tests prove review findings and issue create/update idempotency. The cron trigger was not itself observed during acceptance. OIDC deployment, protected environments, immutable releases, build/SBOM attestations, pre-run attestation enforcement, and a governed query server remain absent. | Show the trusted merge-time checkers, local triage dry run, live disposable issue #26 acceptance record, documented tier substitutes, secret-protection record, and repository AI context. State that the live adapter path was manually invoked and do not claim the cron trigger was observed. Do not claim independent approval or unconditional administrator direct-push prevention under the current policy. Do not claim release provenance, deployment approval, attested containers, or governed assistant data access. |
| [`platform/demo-enablement`](../openspec/changes/add-genomics-variant-accelerator/specs/platform/demo-enablement/spec.md) | **Partially demonstrated** | Presenter safeguards exist in the [claim register](claim-register.md), [known limitations](limitations.md), [demo runbook](demo-runbook.md), coverage table, support statement, and [defect route](../CONTRIBUTING.md#reporting-a-defect). The automated [positioning review](positioning-review.json) scans all presenter-facing Markdown and generated text material against the four claim boundaries, while the machine-readable [assumption register](assumption-register.json) cross-checks every proposal assumption against task, implementation, and coverage evidence. The preflight supports local snapshot validation; the local reset helper diagnoses and clears named SQLite delivery stores without touching infrastructure. The [KPI collector](accelerator-kpis.md) records a local synthetic pipeline history, actual governed-query timestamps, Bronze source-file linkage, and a local synthetic cost ledger before deriving all six KPI values. Parameterized foundation deployment and scoped teardown scripts exist, and a tracked disposable deployment record supports partial storage/staging evidence. The [first-reader dry-run harness](demo-runbook.md#first-reader-dry-run) drove Phases 0, 2, 4, and 5 exactly as published against a scratch directory and matched every documented confirming observation with no undocumented input; the dated [2026-09-19 evidence](dry-run-evidence-2026-09-19.json) records this. Live quota/region validation, end-to-end delivery, cloud reset, teardown cleanliness, production telemetry, and actual Azure billed costs remain unverified, and the dry run itself cannot execute Phases 1 and 6 without an authorized subscription id no document supplies. | Show the local synthetic KPI measurements and the first-reader dry-run evidence only as local, documentation-only evidence. Do not present them as Azure production telemetry, Azure billed cost, a current ready environment, complete reset/teardown, full seven-step demo, or a live bring-up/teardown dry run. |

## Presenter check

Before presenting:

1. Confirm the row still matches the repository and deployed environment.
2. Demonstrate only outputs backed by current implementation evidence.
3. Label unexercised behavior as a production consideration or architectural intent.
4. Follow the [claim register](claim-register.md), especially the product, compliance, customer-reference, and clinical-use boundaries.

Current totals: **0 Demonstrated**, **9 Partially demonstrated**, **1 Specified only**.
