# Capability coverage

This is the source of truth for what the repository can currently support in a presentation. It compares the ten capability specs with implementation evidence that exists in this repository.

OpenSpec implementation progress: **79/89 tasks complete** as of September 19, 2026.

## Status definitions

| Status | Meaning |
|---|---|
| Demonstrated | Runnable behavior exists and can be shown producing the observable results required by the capability spec. |
| Partially demonstrated | Some capability requirements have inspectable or runnable evidence, but the capability is not complete. Show only the evidence named here. |
| Specified only | The repository contains the behavior contract, but no implementation evidence. Describe it only as architectural intent. |

Documentation, diagrams, and specifications are not evidence that data-plane behavior runs. A presenter may show a repository artifact only where the table explicitly identifies that artifact as partial evidence.

## Coverage table

| Capability | Status | Current evidence | Presenter guidance |
|---|---|---|---|
| [`ingestion/smb-landing-zone`](../openspec/changes/add-genomics-variant-accelerator/specs/ingestion/smb-landing-zone/spec.md) | **Partially demonstrated** | The tracked [deployment record](../.azure/deployment-plan.md#historical-verified-results), parameterized [landing-zone Bicep](../infra/modules/landing-zone.bicep), and [acceptance script](../scripts/Test-Environment.ps1) record an SSD provisioned-v2 share with SMB Multichannel, managed-identity mounting, a 100 GiB sequential write measured at 240 MiB/s, and the 3000 IOPS/200 MiB/s provisioned ceiling. The fresh `rg-genomics-20260919` run repeated the private taxonomy and access checks before deallocating the verification VM. The [landing inventory](landing-inventory.md) has runnable local evidence for scheduled discovery, the two-observation or marker completeness rule, and [failure detection and retry](landing-inventory.md#failure-detection-and-retry) against a declared transfer manifest. No real instrument was joined, unchanged instrument configuration and realistic folder seeding are unverified, and no interrupted transfer has been observed over SMB or from an instrument. | Show the tracked acceptance measurements and run the local scanner only within their stated scope, including the synthetic truncated-transfer and retry sequence. Do not claim unchanged lab configuration, a real instrument transfer, or that failure detection works without the sending run declaring an expected size. |
| [`ingestion/object-storage-staging`](../openspec/changes/add-genomics-variant-accelerator/specs/ingestion/object-storage-staging/spec.md) | **Partially demonstrated** | [Data Factory Bicep](../infra/modules/staging.bicep) defines a managed-virtual-network Copy pipeline using separate ingestion and staging identities. The checked-task record and [staging acceptance script](../scripts/Test-Staging.ps1) record a disposable run that copied the stable file and skipped a growing file. The [staging log](../scripts/stage_records.py), its focused tests, and the [integrity acceptance script](../scripts/Test-StagingIntegrity.ps1) cover SHA-256 comparison, the six required status fields, and withholding a deliberately corrupted destination. The fresh group contains the deployed lake account, private endpoints, Data Factory, and Storage Actions task definition; in-network taxonomy and staging-write checks passed. Purview registration is absent, and Storage Actions cannot execute against the policy-isolated HNS account, so no live tier transition exists. | Show the Copy definition, local staging-log behavior, and recorded private storage checks. Do not claim Purview lineage, a lifecycle transition, or end-to-end downstream availability. |
| [`processing/secondary-analysis`](../openspec/changes/add-genomics-variant-accelerator/specs/processing/secondary-analysis/spec.md) | **Partially demonstrated** | The local [Nextflow pipeline](secondary-pipeline.md) and pure-Python orchestrator execute the synthetic workflow through QC, alignment, variant calling and publication, with BAM/CRAM, VCF/GVCF and run provenance on success and stage-level failure handling. Azure Batch and Slurm/HPC profiles are configuration preparation only. No live executor run, concordance result, or compute release measurement exists. | Show the local synthetic execution and failure record. Present Batch and HPC as prepared but unverified targets. Do not claim biological validation, equivalent executor outputs, live cloud execution, or elastic scale-down. |
| [`variant-store/delta-variant-store`](../openspec/changes/add-genomics-variant-accelerator/specs/variant-store/delta-variant-store/spec.md) | **Partially demonstrated** | The local [SQLite Bronze harness](variant-store.md), `scripts/variant_store.py`, and focused synthetic tests implement the exact 20-field contract, VCF parsing, mandatory-field rejection, null annotations, run/reference provenance, rejected records, idempotency, reprocessing separation, shortcut metadata resolution, and inspectable layout/maintenance metadata. It is not a deployed Delta table, OneLake shortcut, ADLS tier transition, or production optimization run. | Show only the local synthetic parser/store behavior and keep the exact VCF core and context-field selection labelled as an accelerator assumption. Do not claim deployed Delta, cloud tiering, production layout validation, or representative-volume performance. |
| [`variant-store/genomic-metadata`](../openspec/changes/add-genomics-variant-accelerator/specs/variant-store/genomic-metadata/spec.md) | **Partially demonstrated** | The local [SQLite metadata model](metadata-store.md) traverses the synthetic Subject-to-variant chain in both directions, enforces required file metadata and referential integrity, supports metadata-only archival, and implements separate clinical/research grants with subject-linkage filtering. It is a trusted local API, not a deployed or access-controlled service. Payload archival, cloud authorization, and Delta/Purview integration remain unverified. | Demonstrate only the synthetic local model and label URI, producer, integrity values, grants and audit behavior as local evidence. Do not claim a deployed service, storage archival, cloud authorization or clinical linkage. |
| [`reference-data/reference-management`](../openspec/changes/add-genomics-variant-accelerator/specs/reference-data/reference-management/spec.md) | **Partially demonstrated** | The [reference publisher](../scripts/publish_reference.py) and focused tests implement `type/name/version` publication, per-artifact SHA-256 manifests, inventory listing, and refusal to republish an existing version. The checked-task [verification record](../openspec/changes/add-genomics-variant-accelerator/tasks.md#4-reference-data) and [acceptance script](../scripts/Test-ReferenceData.ps1) record five versioned Ensembl artifacts published in an earlier disposable reference container, a WORM-policy overwrite rejected with `409 BlobImmutableDueToPolicy`, and a successor version leaving the prior retrievable. [Submission-time compatibility validation](../docs/reference-submission.md) and [audited publish/read authorization](governance-local-controls.md#reference-publish-and-read) have local tests, including a denied publish that leaves the zone unchanged. The Azure client drivers still publish with an explicit `None` governor, so cloud-side reference auditing is not implemented. The fresh group contains a lake account, but fresh reference publication and availability were not accepted. | Show the local publisher, compatibility, and audit tests and the tracked earlier disposable publication/immutability results. Name the published entries exactly as recorded; do not relabel GRCh37 as a tested hg19 distribution or claim that the current deployment's reference publication is available or audited. |
| [`governance/access-and-lineage`](../openspec/changes/add-genomics-variant-accelerator/specs/governance/access-and-lineage/spec.md) | **Partially demonstrated** | The tracked infrastructure defines separate managed identities, scoped storage roles, private endpoints, private DNS, disabled shared-key access, and private Data Factory connectivity. The local controls implement four access tiers, de-identified projections, clinical/research grants, classification, append-only hash-chained audit, reference authorization and the external-sharing approval gate. [Local lineage](lineage.md) joins variant, metadata and staging records with a Microsoft Purview catalog-context simulation marked `live: false`. The fresh group's in-network acceptance passed the private taxonomy, staging-write, processing read/write separation, landing-share denial, and managed-identity SMB checks. No private governed query endpoint, service-side audit store, or live Purview lineage is deployed. | Present the local controls and recorded private storage-path checks within their measured scope. Do not claim compliance, a complete private end-to-end path, live Purview lineage, or a deployed sharing boundary. |
| [`analytics/variant-query-and-visualization`](../openspec/changes/add-genomics-variant-accelerator/specs/analytics/variant-query-and-visualization/spec.md) | **Partially demonstrated** | The local [governed analytics harness](analytics-query.md), `scripts/analytics_query.py`, `scripts/analytics_experience.py`, and focused synthetic tests implement all six query scenarios, access-aware projections, equivalent notebook-style and SQL-adapter results, per-row traceability, snapshot reruns, six view models, measured local response time, and bounded AI-assisted exploration through the governed query engine. The 1,000 ms target and screen set remain assumptions; no Fabric/Databricks notebook, SQL warehouse, dashboard, or governed AI service is deployed. | Show the local synthetic query/view/assistant behavior and its exploratory label. Keep the target, screen set, scenario, and service choices labelled as assumptions. Do not present local adapters or view models as deployed Azure analytics services or clinical interpretation. |
| [`platform/engineering-workflow`](../openspec/changes/add-genomics-variant-accelerator/specs/platform/engineering-workflow/spec.md) | **Partially demonstrated** | The required data-hygiene and secret scans, repository AI context, trusted PR review, deterministic failure triage, account-tier controls and live synthetic issue acceptance are documented in [engineering controls](engineering-controls.md). Immutable release `v0.2.1-pipeline` is live and verified. Its workflow authenticated to Azure through OIDC, pushed the pipeline image to ACR, and published registry-backed SLSA provenance and CycloneDX SBOM attestations. A stored synthetic variant was traced through the release and image digest to the signed aligner and variant-caller versions. Pipeline submission also rejects missing attestation evidence before allocation. | Show the immutable release, OIDC workflow run, ACR verification, variant trace, and issue #26 acceptance. State that triage was manually invoked. Do not claim independent protected-environment approval or a network-governed assistant endpoint. |
| [`platform/demo-enablement`](../openspec/changes/add-genomics-variant-accelerator/specs/platform/demo-enablement/spec.md) | **Partially demonstrated** | Presenter safeguards exist in the [claim register](claim-register.md), [known limitations](limitations.md), [demo runbook](demo-runbook.md), coverage table, support statement, and [defect route](../CONTRIBUTING.md#reporting-a-defect). The automated [positioning review](positioning-review.json) scans all presenter-facing Markdown and generated text material against the four claim boundaries, while the machine-readable [assumption register](assumption-register.json) cross-checks every proposal assumption against task, implementation, and coverage evidence. The preflight supports local snapshot validation; the local reset helper diagnoses and clears named SQLite delivery stores without touching infrastructure. The [KPI collector](accelerator-kpis.md) records a local synthetic pipeline history, actual governed-query timestamps, Bronze source-file linkage, and a local synthetic cost ledger before deriving all six KPI values. Parameterized foundation deployment and scoped teardown scripts exist, and a tracked disposable deployment record supports partial storage/staging evidence. The [first-reader dry-run harness](demo-runbook.md#first-reader-dry-run) drove Phases 0, 2, 4, and 5 exactly as published against a scratch directory and matched every documented confirming observation with no undocumented input; the dated [2026-09-19 evidence](dry-run-evidence-2026-09-19.json) records this. Live quota/region validation, end-to-end delivery, cloud reset, teardown cleanliness, production telemetry, and actual Azure billed costs remain unverified, and the dry run itself cannot execute Phases 1 and 6 without an authorized subscription id no document supplies. | Show the local synthetic KPI measurements and the first-reader dry-run evidence only as local, documentation-only evidence. Do not present them as Azure production telemetry, Azure billed cost, a current ready environment, complete reset/teardown, full seven-step demo, or a live bring-up/teardown dry run. |

## Presenter check

Before presenting:

1. Confirm the row still matches the repository and deployed environment.
2. Demonstrate only outputs backed by current implementation evidence.
3. Label unexercised behavior as a production consideration or architectural intent.
4. Follow the [claim register](claim-register.md), especially the product, compliance, customer-reference, and clinical-use boundaries.

Current totals: **0 Demonstrated**, **10 Partially demonstrated**, **0 Specified only**.
